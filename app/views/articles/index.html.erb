<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles!

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
          <%= link_to article.title, article %> | <%= link_to "Edit", edit_article_path(article) %> | <%= render "delete", article: article %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>